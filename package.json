{
  "name": "blancbleu-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo BlancBleu : server (Node) + client (React) + ai-service (Python) + 2 apps Flutter — tooling racine (Husky, lint-staged, Playwright E2E).",
  "scripts": {
    "prepare": "husky",
    "e2e": "playwright test",
    "lint:server": "npm --prefix server run lint",
    "lint:client": "npm --prefix client run lint",
    "lint": "npm run lint:server && npm run lint:client",
    "test:server": "npm --prefix server test",
    "test:client": "npm --prefix client test -- --watchAll=false"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.5"
  }
}
